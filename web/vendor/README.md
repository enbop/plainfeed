# Vendored frontend dependency

`htmx.min.js` is htmx 2.0.10, vendored so the reader has no CDN or Node runtime
dependency. Its upstream source and license are at
<https://github.com/bigskysoftware/htmx>.
